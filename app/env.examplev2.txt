# --- AWS CONFIG ---
AWS_ACCESS_KEY_ID=TU_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=TU_SECRET_ACCESS_KEY
AWS_SESSION_TOKEN=TU_SESSION_TOKEN   # opcional si usás sesión temporal
AWS_REGION=us-east-1                 # región donde desplegarás (ej: us-east-1)

# --- EC2 CONFIG ---
KEY_NAME=mi-clave-ec2                 # nombre del par de claves
AWS_IMAGE_ID=ami-0c55b159cbfafe1f0    # ID de la AMI (ej: Amazon Linux 2023)
AWS_INSTANCE_TYPE=t2.micro            # tipo de instancia
AWS_EC2_NAME=mi-app-php               # nombre/tag de la instancia
SG_EC2_NAME=sg-ec2-php                # nombre del security group para EC2

# --- RDS CONFIG ---
DB_IDENTIFIER=mi-rds-db               # identificador único de la instancia RDS
DB_INSTANCE_CLASS=db.t3.micro         # clase de instancia RDS
DB_ENGINE=mysql                       # motor de base de datos
DB_MASTER_USER_NAME=admin             # usuario administrador
DB_MASTER_PASSWORD=SuperPassword123!  # contraseña administrador
DB_APP_NAME=app_db                    # nombre de la base de datos
SG_DB_NAME=sg-rds-db                  # nombre del security group para RDS

# --- APP CONFIG (PHP) ---
DB_PORT=3306                          # puerto de la base de datos
DB_APP_USER=appuser                   # usuario de la app en la DB
DB_APP_PASSWORD=AppPassword123!       # contraseña de la app en la DB

APP_ADMIN_USER=admin                  # usuario administrador de la app
APP_ADMIN_PASSWORD=AdminPassword123!  # contraseña administrador de la app

APP_REPO_URL=https://github.com/usuario/repositorio-php.git  # URL del repo de la app PHP